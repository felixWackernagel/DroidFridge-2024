<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="de.wackernagel.droidfridge.fragment.AddProductFragment">
    <data>
        <variable
            name="viewModel"
            type="de.wackernagel.droidfridge.viewmodel.AddProductViewModel" />
    </data>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <androidx.appcompat.widget.LinearLayoutCompat
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/MyApplication_LinearLayoutCompat">
            <include
                android:id="@+id/name_text_field_binding"
                app:viewModelValue="@={viewModel.newProduct.name}"
                layout="@layout/text_input_name" />
            <include
                app:viewModelFunc="@{ () -> viewModel.addProduct() }"
                android:id="@+id/crud_buttons"
                layout="@layout/crud_buttons" />
        </androidx.appcompat.widget.LinearLayoutCompat>
    </ScrollView>
</layout>